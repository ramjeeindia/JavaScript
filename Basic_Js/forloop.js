// for (intial expression; condition; update expression){// for loop body}


// for (let index = 0; index < array.length; index++) {
//     const element = array[index];
    
// }

// print 0 to 10 number

for (let index = 0; index <=10; index++) {
    console.log(index);
 }

//  we can also write 

// for (let index = 0; index <=10; ) {index++
//     console.log(index);
//  }

for (let id = 0; id<=10; id++){
    for(let maxid = 10; maxid <=15; maxid++){
        console.log(maxid);
    }
    console.log(id);
}